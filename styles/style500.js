import { styled } from '@mui/material/styles';

const ErrorContainer = styled('div')({
    padding: 4,
});

const Title = styled('h1')({
    marginBottom: 2,
});

const ErrorMessage = styled('p')({
    color: '#fff',
});

export { ErrorContainer, ErrorMessage, Title };
